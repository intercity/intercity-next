<h1>New server</h1>
<p><%= icon "warning" %>The server your are adding here needs to have Dokku installed already!</p>
<%=  bootstrap_form_for @server do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :ip %>
  <%= f.submit "Create server" %>
<% end %>
