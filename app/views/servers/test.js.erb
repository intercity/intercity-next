<% if @server.setup? %>
    new ServerPoller("<%= test_server_path(@server) %>").poll();
<% elsif @server.up? %>
  $("[data-server-status~=checking]").addClass("hidden");
  $("[data-server-status~=up]").removeClass("hidden");
<% end %>
